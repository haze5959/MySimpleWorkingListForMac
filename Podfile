# Uncomment the next line to define a global platform for your project
platform :osx, '10.9'

target 'MyWorkingListForMac' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyWorkingListForMac
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'KeyHolder'

  target 'MyWorkingListForMacTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
